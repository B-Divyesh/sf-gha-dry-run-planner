# No-execution GitHub Actions workflow planner: which jobs/steps will run for a given event, and why

Live: https://gha-dry-run-planner.sociobot.in — built by the Param Factory (`cli`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
